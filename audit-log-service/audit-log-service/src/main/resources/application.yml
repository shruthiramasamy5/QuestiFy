server:
  port: 8090

spring:
  application:
    name: audit-log-service
  mongodb:
    uri: ${SPRING_MONGODB_URI:${MONGODB_URI:mongodb+srv://shruthiramasamy5_db_user:ZKxaL4MlT420PlOF@cluster0.cwqbldx.mongodb.net/audit_log_service?retryWrites=true&w=majority}}
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:${SPRING_MONGODB_URI:${MONGODB_URI:mongodb+srv://shruthiramasamy5_db_user:ZKxaL4MlT420PlOF@cluster0.cwqbldx.mongodb.net/audit_log_service?retryWrites=true&w=majority}}}
      auto-index-creation: true

eureka:
  client:
    enabled: ${EUREKA_CLIENT_ENABLED:false}
    service-url:
      defaultZone: ${EUREKA_SERVER_URL:${EUREKA_URL:${EUREKA_URI:http://localhost:8761/eureka/}}}
  instance:
    prefer-ip-address: true

questify:
  jwt:
    secret: ${QUESTIFY_JWT_SECRET:${JWT_SECRET:questify-dev-secret-key-change-me-before-deploying-anywhere-real}}

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.questify.audit: INFO
